# RescueCast Windows 本地运行

请始终只使用解压后的 `RescueCast-Local` 文件夹，不要再复制出其他项目目录。

## 第一次运行

1. 安装 Node.js 22.13 或更高版本。
2. 解压压缩包，进入 `RescueCast-Local`。
3. 双击 `setup-local.bat`，等待出现 `SETUP COMPLETE`。脚本只使用英文输出，避免 Windows CMD 中文编码乱码。
4. 双击 `start-local.bat`。
5. 浏览器打开 `http://127.0.0.1:3000`。

## 以后启动

以后只需要双击 `start-local.bat`。如果依赖不完整，它会自动重新执行安装。网站运行期间不要关闭黑色命令窗口；需要停止时，在窗口中按 `Ctrl+C`。

## 建议测试

- 输入“北京市朝阳区朝阳大悦城”，确认地图移动到对应商场。
- 尝试拖动、缩放地图。
- 切换事件类型，并测试“自定义事件”。
- 选择发生时间，生成应急通告。
- 上传一张现场图片。
- 点一次“有帮助”或“需改进”，刷新后确认记录仍保留。

本地数据保存在项目内的 `.wrangler` 文件夹中。删除该文件夹会清空本地评价与图片记录，但不会影响公开网站的数据。
